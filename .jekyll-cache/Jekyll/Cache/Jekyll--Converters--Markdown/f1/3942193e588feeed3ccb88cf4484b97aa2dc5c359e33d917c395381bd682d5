I"�<p>여기서 <code class="language-plaintext highlighter-rouge">encodeData</code>는 <code class="language-plaintext highlighter-rouge">enc(value₁) ‖ enc(value₂) ‖ … ‖ enc(valueₙ)</code>를 의미하는데, 각 value는 정확히 32 바이트 길이여야한다. 즉 <code class="language-plaintext highlighter-rouge">bytes</code> 혹은 <code class="language-plaintext highlighter-rouge">string</code>과 같은 dynamic 값들은 keccak256 함수를 적용해 인코딩함으로써 32 바이트 길이로 통일시켜줘야한다. 배열의 경우에는 배열 값들을 concatenate하여 데이터 값에 keccak256 함수를 적용한다 (예시로 uint256 arr[3]의 경우, keccak256(arr[0]</p>
:ET